// D12 · Drag / Terminal Velocity (L12) — an application of Newton's laws.
//
// Drag and terminal velocity belong with the applications-of-Newton chapter (next
// to friction), not with Chapter 2 kinematics. The implementation is shared with
// the Free Fall family (same fall integrator), so this re-exports that view as its
// own demo in the correct chapter.
export { DragMode as default } from '../ch02-motion-1d/FreeFall';
