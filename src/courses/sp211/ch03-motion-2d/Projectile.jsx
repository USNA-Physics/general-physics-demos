// D06 · Projectile Motion (L6) — two-dimensional kinematics.
//
// Projectile motion belongs with Chapter 3 (Motion in Two Dimensions), so this is
// its home in the nav and lesson index. The implementation is shared with the
// Free Fall family (it reuses the same trajectory/integrator code), so this file
// re-exports that view as its own demo.
export { ProjectileMode as default } from '../ch02-motion-1d/FreeFall';
